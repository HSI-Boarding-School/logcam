# VITE_API_BASE=http://localhost:8000
# VITE_WS_BASE=ws://localhost:8000

## OrbStack Dev Domain (HTTPS) — use same-origin via Nginx
VITE_API_BASE=/api
VITE_WS_BASE=
VITE_ALLOWED_HOSTS=nginx.logcam.orb.local,logcam.local

## OrbStack Dev Domain (absolute URLs)
# VITE_API_BASE=https://logcam.local/api
# VITE_WS_BASE=wss://logcam.local
# VITE_ALLOWED_HOSTS=nginx.logcam.orb.local,logcam.local
